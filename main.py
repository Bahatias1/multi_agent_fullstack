from orchestrator.super_agent import run_super_agent

if __name__ == "__main__":
    result = run_super_agent(
        "Crée une API FastAPI avec authentification JWT et structure propre"
    )
    print(result)
